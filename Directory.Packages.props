<!-- SPDX-License-Identifier: Apache-2.0 -->
<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <!-- Versions consumed by later projects (tests, benchmarks). Harmless if unused. -->
  <ItemGroup>
    <PackageVersion Include="xunit.v3" Version="3.2.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.7.0" />
    <PackageVersion Include="CsCheck" Version="4.7.0" />
    <PackageVersion Include="BenchmarkDotNet" Version="0.15.8" />
    <PackageVersion Include="pocketken.H3" Version="4.0.0" />
    <!-- Matches pocketken.H3 4.0.0's own NetTopologySuite dependency; the benchmarks
         build pocketken's NTS-based polyfill input (Polygon) directly. -->
    <PackageVersion Include="NetTopologySuite" Version="2.5.0" />
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="Microsoft.CodeAnalysis.PublicApiAnalyzers" Version="3.3.4" />
  </ItemGroup>

  <!-- Applied to every project; build/dev assets only, never become package dependencies. -->
  <ItemGroup>
    <GlobalPackageReference Include="MinVer" Version="7.0.0" />
    <GlobalPackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" Version="10.0.301" />
    <GlobalPackageReference Include="Meziantou.Analyzer" Version="3.0.117" />
  </ItemGroup>

</Project>
